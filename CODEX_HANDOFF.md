# Codex 引き継ぎ — 横動画ショート変換プログラム

## 1. プロジェクトの目的

既に制作・公開済みの長編16:9動画から、YouTube Shorts / TikTok / Instagram Reels向けに使える縦型ショート動画を半自動〜最終的には全自動で生成するローカルWebアプリを作る。

重要なのは、単純な9:16変換ではない。
AIが長編全体を理解し、「前後の文脈なしでも成立する」「冒頭の引きが強い」「驚き・比較・結論・失敗・レビュー・デメリット等がある」「30〜60秒前後で完結する」といった観点で候補区間を抽出・採点し、そこから実際のショート動画を書き出すことがコア価値。

## 2. 利用イメージ

1. ユーザーがブラウザUIを開く
2. 長編動画をドラッグ＆ドロップ / 選択
3. 「ショート候補を解析」を押す
4. 音声を文字起こしし、AIが候補を5〜10件ランキング
5. UIに以下を表示
   - 開始 / 終了時刻
   - 尺
   - 内容の要約
   - ショート適性スコア（0〜100）
   - 選定理由
   - 推奨フック案
6. ユーザーが候補を選択、または「全部生成」
7. 9:16化・字幕・必要に応じたクロップ追尾を行いMP4出力

最終ゴールは「長編動画を入れてボタンを押すだけ」で複数の完成ショートが生成される状態。

## 3. 運用要件

- ソースコードはGitHubで管理する
- 複数PCから編集・更新できる
- macOS / Windows両対応
- 妻のWindows PCでも簡単に使えること
- 操作UIはブラウザベース
- 動画処理は原則ローカルPC内で実行する
- 元動画や生成動画はGitHubへコミットしない
- 非エンジニアでも起動しやすいこと
  - Windows: `run_windows.bat`
  - macOS: `run_mac.command`
- GitHub Desktopでpull/pushする運用も想定

## 4. 現在の技術構成

- Python
- FastAPI
- HTML / JavaScript のローカルWeb UI
- FFmpeg / ffprobe
- ローカルファイル保存

現在の依存関係:

```text
fastapi==0.116.1
uvicorn[standard]==0.35.0
python-multipart==0.0.20
```

## 5. 現在の実装（v0.6）

実装済み:

- FastAPIサーバー
- ブラウザUI
- 動画ドラッグ＆ドロップ / upload API
- 対応動画形式チェック
- `ffmpeg` / `ffprobe` 存在確認
- ffprobeによる動画メタデータ取得
- job ID発行
- `data/jobs/{job_id}.json` へのジョブ保存
- `data/uploads/` への動画保存
- Mac / Windows用の起動スクリプト
- 動画・生成物・ローカルデータをGitから除外
- faster-whisperによるタイムコード付き日本語文字起こし
- 文字起こしのバックグラウンド処理・進捗表示・JSON保存
- 30〜60秒を中心としたショート候補のランキング
- OpenAI Responses APIによるAI評価（APIキー設定時）
- APIキー未設定時のローカル候補評価
- 候補のスコア・要約・選定理由・フック案表示
- 候補ごとの1080x1920 MP4書き出し
- 複数候補の見どころを抽出・連結する再編集
- 完成動画のブラウザ再生・保存
- Whisperタイムコードの日本語字幕焼き込み
- 個別切り抜き・再編集後の字幕時刻再計算
- 候補別の動画上部固定タイトル（文章・サイズ・上下位置）と、字幕文章・サイズ・位置の編集
- 候補範囲の前後調整と、削除した字幕区間に連動する映像・音声カット／自動連結
- 候補ごとの9:16クロップ横位置（左・中央・右）選択
- 完成動画ファイルと履歴を画面から個別削除
- 固定タイトル・字幕テロップの手動改行を保持
- テロップ内の半角・全角スペースを保持
- 固定タイトル最大サイズ200、最終区間だけの締めテロップ設定
- 完成動画ごとの投稿タイトル・概要欄・ハッシュタグ自動生成
- YouTube素材の元概要欄・タイトル・チャンネル・URLを利用した投稿概要欄生成
- YouTube 403対策としてIPv4・Node.js・互換MP4優先・段階的な自動再試行

主なAPI:

```text
GET  /api/health
POST /api/upload
GET  /api/jobs/{job_id}
POST /api/jobs/{job_id}/transcribe
POST /api/jobs/{job_id}/analyze-candidates
```

現在の `upload` 完了時ステータス:

```json
{
  "status": "uploaded",
  "next_step": "transcription"
}
```

次の実装地点は、候補を選択して9:16動画に書き出すv0.4。

## 6. ディレクトリ構成

```text
yokodouga-short-converter/
├── app/
│   ├── main.py
│   └── static/
│       └── index.html
├── data/
│   ├── uploads/
│   ├── outputs/
│   └── jobs/
├── .env.example
├── .gitignore
├── requirements.txt
├── README.md
├── CODEX_HANDOFF.md
├── run_mac.command
└── run_windows.bat
```

## 7. 開発ロードマップ

### v0.2 — タイムコード付き文字起こし【実装済み】

長編動画から音声を取得し、日本語を中心にタイムコード付きで文字起こしする。

要件:

- ローカルで実行できるWhisper系実装を優先
- Mac / Windows両対応
- CPUでも動くこと（GPUがあれば高速化できる設計は歓迎）
- 文字起こし結果はジョブ単位でJSON保存
- セグメント単位で最低限以下を保持

```json
{
  "start": 12.34,
  "end": 18.91,
  "text": "実際に使ってみて一番驚いたのがここです"
}
```

- UIから文字起こし開始できる
- 進行状況 / statusを表示できる
- エラー内容をユーザーに分かる形で返す
- 既存のv0.1を壊さない

候補ライブラリは `faster-whisper` を第一候補として検討してよい。ただしMac/Windowsのセットアップ容易性・依存関係・モデルDLを含めて判断すること。

### v0.3 — ショート候補AI抽出・ランキング【実装済み】

文字起こしをLLMに渡し、5〜10候補を抽出。

各候補の出力:

```json
{
  "start": 522.0,
  "end": 567.0,
  "duration": 45.0,
  "score": 94,
  "summary": "ARグラスとQuest 3の画質差についての本音",
  "reason": "比較・結論が45秒で完結し、冒頭に強い主張がある",
  "hook": "実はQuest 3よりこっちの方が綺麗に見えるんです"
}
```

採点基準:

- 前後の文脈なしで理解できる
- 冒頭3秒に強いフックを置ける
- 30〜60秒程度を優先（絶対条件ではない）
- 結論 / オチ / 明確な価値がある
- 驚き、比較、本音、失敗、意外性、デメリット、How-to等を評価
- 同じ内容の候補を重複させない
- 単なる挨拶・スポンサー説明・冗長な前置きは低評価

### v0.4 — 自動切り抜き & 9:16書き出し【実装済み】

- FFmpegで指定区間を切り抜く
- 1080x1920 MP4
- 元映像の単純中央クロップをまず実装
- 音声は維持
- 候補ごとに `data/outputs/` に保存

### v0.5 — 人物/顔追尾クロップ

- 横動画内の話者を追尾
- 顔が左右に移動しても9:16内に維持
- 複数人時の挙動を考慮
- 顔がないシーンでは中央 / 注目領域などへフォールバック

### v0.6 — Shorts向け字幕【実装済み】

- Whisperタイムコードを利用
- 1画面の文字量を抑える
- 日本語として自然な区切り
- 画面下部〜中央下に大きく読みやすく表示
- 将来的にテンプレート / フォント / サイズ変更可能にする

### v0.7 — テンポ自動編集

- 長い無音を短縮
- 「えー」「あのー」等の不要フィラー処理（やりすぎない）
- 言い直し候補の除去
- 必要に応じて冒頭へ強いフック部分を移動

### v1.0 — ワンクリック生成

- 動画投入
- AI解析
- 候補選定
- 縦化
- 字幕
- 書き出し
- 複数ショート完成

までブラウザから一連で行える。

## 8. UI方針

非エンジニアが迷わず使えることを最優先。

最終イメージ:

```text
[ 動画をドラッグ＆ドロップ ]

XREAL_review.mp4
24:18 / 1920x1080

[ ショート候補を解析 ]

解析結果
--------------------------------------------------
☑ #1  08:42 - 09:27  45秒  94点
   ARグラスとQuest 3の画質比較
   「比較・結論が単体で成立」

☑ #2  13:15 - 13:52  37秒  91点
   実際に使って驚いたポイント
--------------------------------------------------

[ 選択したショートを生成 ] [ 全部生成 ]
```

技術者向けの設定やログは折りたたみ / 詳細設定へ逃がし、基本画面を複雑にしない。

## 9. Git / データ管理ルール

GitHubへ入れる:

- Python / HTML / JS / CSS
- 設定サンプル
- README
- テスト
- ドキュメント

GitHubへ入れない:

- 元動画
- 音声一時ファイル
- Whisperモデル
- 生成動画
- APIキー
- job実データ

`.gitignore` を維持・必要に応じて強化すること。

## 10. セキュリティ / プライバシー方針

- ユーザーの動画は原則ローカル処理
- クラウドLLMを候補選定に使う場合、文字起こしテキストのみ送信する構成を基本とする
- APIキーは `.env` に置き、Gitへ絶対にコミットしない
- 将来的にはLLM部分もローカル / クラウド切替可能だと望ましい

## 11. Codexへの直近タスク

次は **v0.5（人物/顔追尾クロップ）を完成させる**。

実装前に既存コード全体を読み、既存の起動方法・API・UIを壊さないこと。

完了条件:

1. 候補から生成対象をブラウザで選択できる
2. FFmpegで指定区間を正確に切り抜ける
3. 1080x1920の9:16 MP4を中央クロップで生成できる
4. 元動画の音声を維持できる
5. 候補ごとに `data/outputs/` へ保存される
6. 進捗と失敗理由がブラウザに表示される
7. 既存のアップロード・文字起こし・候補解析を壊さない
8. 生成動画をブラウザから確認できる
9. 生成物がGitに入らない
10. 最低限の動作確認 / テストを実施する

できれば処理中にHTTPリクエストを長時間ブロックしない設計にする。MVPなので過剰なインフラは不要だが、バックグラウンド処理 + job statusポーリング等のシンプルな構成を検討する。

## 12. Codexに最初に送る指示（コピペ用）

以下をそのままCodexへの最初の依頼として使用可能:

> このリポジトリは、長編16:9動画からAIがYouTube Shorts / TikTok / Instagram Reels向けの見どころを選び、自動で縦動画化するローカルWebアプリです。まず `README.md`、`CODEX_HANDOFF.md`、既存コードをすべて確認してください。実装済みのv0.3を壊さず、「v0.4 — 自動切り抜き & 9:16書き出し」と「Codexへの直近タスク」の完了条件に沿って実装してください。MacとWindowsの両方で使えること、非エンジニアでもブラウザから操作できること、元動画はローカル処理することを優先してください。実装後は変更内容、動作確認結果、未解決事項を簡潔にまとめてください。

## 13. 今後の設計で特に守りたいこと

このプロジェクトの成功基準は「AIを使っていること」ではなく、実際のYouTube制作時間が減ること。

そのため:

- 精度より先にワークフローを成立させる
- 各段階を手動確認できるようにする
- いきなり完全自動化せず、候補表示→選択→生成の順で育てる
- Windowsでもセットアップ地獄にならないこと
- UIは極力シンプルにする
- モジュールを分け、後から文字起こしエンジンやLLMを交換しやすくする

を優先する。
